        Truncate Table  RPACCESS Reuse Storage;

        Truncate Table  RPACCESS_FIELD Reuse Storage;

        Truncate Table  RPASSIGNMENT Reuse Storage;

        Truncate Table  RPBLOB        Reuse Storage;

        Truncate Table  RPCALLED_MODULE       Reuse Storage;

        Truncate Table  RPCODE_BLOCK  Reuse Storage;

        Truncate Table  RPCOMMENT     Reuse Storage;

        Truncate Table  RPCONDITION   Reuse Storage;

        Truncate Table  RPCONDITION_DETAIL    Reuse Storage;

        Truncate Table  RPCOORDINATE  Reuse Storage;

        Truncate Table  RPCURSOR      Reuse Storage;

        Truncate Table  RPCURSOR_KEY  Reuse Storage;

        Truncate Table  RPEVENT       Reuse Storage;

        Truncate Table  RPEVENT_DETAIL        Reuse Storage;

        Truncate Table  RPEXTERNAL    Reuse Storage;

        Truncate Table  RPFIELD_PROPERTY      Reuse Storage;

        Truncate Table  RPFIELD_VALUE         Reuse Storage;

        Truncate Table  RPFUNCTION    Reuse Storage;

        Truncate Table  RPHIGHLIGHT   Reuse Storage;

        Truncate Table  RPHIGHLIGHT_DETAIL    Reuse Storage;

        Truncate Table  RPINDEX_SEGMENT       Reuse Storage;

        Truncate Table  RPLITERAL     Reuse Storage;

        Truncate Table  RPLITERAL_summary     Reuse Storage;

        Truncate Table  RPLOG_FILE    Reuse Storage;

        Truncate Table  RPLOOKUP      Reuse Storage;

        Truncate Table  RPMESSAGE     Reuse Storage;

        Truncate Table  RPMETHOD      Reuse Storage;

        Truncate Table  RPMODULE      Reuse Storage;

        Truncate Table  RPOBJECT      Reuse Storage;

        Truncate Table  RPPANEL       Reuse Storage;

        Truncate Table  RPPASSING_LIST        Reuse Storage;

        Truncate Table  RPPROCEDURE   Reuse Storage;

        Truncate Table  RPPROCEDURE_DETAIL    Reuse Storage;

        Truncate Table  RPRECEIVING_LIST      Reuse Storage;

        Truncate Table  RPRELATION    Reuse Storage;

        Truncate Table  RPRELATION_FIELD      Reuse Storage;

        Truncate Table  RPRELATION_INDEX      Reuse Storage;

        Truncate Table  RPREQUEST     Reuse Storage;

        Truncate Table  RPSCREEN_PROPERTY     Reuse Storage;

        Truncate Table  RPSET_OPTION  Reuse Storage;

        Truncate Table  RPSORT_BREAK  Reuse Storage;

        Truncate Table  RPSORT_OPTION         Reuse Storage;

        Truncate Table  RPSTACK       Reuse Storage;

        Truncate Table  RPSTACK_DETAIL        Reuse Storage;

        Truncate Table  RPSTORED_PROCEDURE    Reuse Storage;

        Truncate Table  RPSUMINTO     Reuse Storage;

        Truncate Table  RPTRANSACTION         Reuse Storage;

        Truncate Table  RPTRANSACTION_RELATION                                                   Reuse Storage;

        Truncate Table  RPUPDATE      Reuse Storage;

        Truncate Table  RPUSER        Reuse Storage;

        Truncate Table  RPVARIABLE    Reuse Storage;

        Truncate Table  RPModule_Metric Reuse Storage;

        Truncate Table  RPSort_Option_Summary Reuse Storage;

	Truncate Table  RPPanel_Summary Reuse Storage;

	Truncate Table  RPField_Property_Summary Reuse Storage;

	Truncate Table  RPAssignment_Summary Reuse Storage;
	
	Truncate Table  RPField_Prop_Format_Summary Reuse Storage;
	
	Truncate Table  RPModule_Bridge Reuse Storage;

        Truncate Table  error_log Reuse Storage;
