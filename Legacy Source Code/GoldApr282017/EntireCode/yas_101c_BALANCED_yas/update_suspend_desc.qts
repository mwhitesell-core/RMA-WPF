can clear
set verify errors
set default
set process nolimit
set lock file update

request update_f002_suspend_desc

access f002-suspend-desc

select if clmdtl-doc-ohip-nbr = 133025

output f002-suspend-dtl update on errors report
    item clmdtl-status final "D"

build $obj/update-suspend-desc
