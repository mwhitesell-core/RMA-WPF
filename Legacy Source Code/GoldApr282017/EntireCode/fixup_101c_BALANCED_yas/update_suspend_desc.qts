can clear
set verify errors
set default
set process nolimit
set lock file update

request update_f002_suspend_desc

access f002-suspend-desc

select if clmdtl-doc-ohip-nbr = 0

output f002-suspend-desc update on errors report
    item clmdtl-doc-ohip-nbr final 017128
;   item clmdtl-status       final "D"

build $obj/update-suspend-desc
