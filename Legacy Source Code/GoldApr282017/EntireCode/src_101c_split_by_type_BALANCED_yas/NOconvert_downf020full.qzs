cancel clear
set default
set rep nolimit
set nohead
set rep dev disc name downf020full

access f020-doctor-mstr

select if doc-full-part-ind = "F"

def x-doc-name char*27 = lj(truncate(doc-name)) + ", " + pack(doc-inits)
sort on doc-name
rep x-doc-name
  
