; 2014/oct/22	MC	delete_f001.qts
;			this program should only be run when there is error 'invalid clinic in constants mstr'
;			when running r001b or r001

cancel clear
set lock record update

request one
access *badbatch link to f001-batch-control-file

subfile savef001 keep include f001-batch-control-file

output f001-batch-control-file delete on errors report
go

exit
