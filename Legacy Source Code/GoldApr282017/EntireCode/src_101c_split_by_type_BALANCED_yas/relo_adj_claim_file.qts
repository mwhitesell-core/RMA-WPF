cancel clear
set process nolimit

access *unlo_adj_claim_file

def x-batch-nbr char*8 = ascii(adj-batch-nbr,9)[1:2] + ascii(adj-batch-nbr,9)[4:6]

output adj-claim-file add on errors report
   item adj-batch-nbr final x-batch-nbr

build $obj/relo_adj_claim_file
