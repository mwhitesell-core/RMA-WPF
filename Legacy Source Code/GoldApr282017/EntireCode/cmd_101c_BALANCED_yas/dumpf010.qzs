cancel clear
set rep nolimit
set rep nohead

access f010-pat-mstr

rep subscr-addr1 skip subscr-addr2 skip subscr-addr3 skip subscr-prov-cd subscr-postal-cd &
skip "------"

def x char*1= subscr-postal-cd[1:1]
select if (x < "A" or x > "Z") and x <> " " and x <> "0" and x <> "."
go
