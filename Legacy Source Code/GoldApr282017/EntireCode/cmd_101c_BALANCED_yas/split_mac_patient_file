cp				 mac_patient_file mac_patient_file.orig
echo "Ensure the $cmd/split_mac.awk has the correct macxx filenames !"
echo "Press newline to continue ..."
read garbage

rm mac.???

awk -f $cmd/split_mac.awk < mac_patient_file
