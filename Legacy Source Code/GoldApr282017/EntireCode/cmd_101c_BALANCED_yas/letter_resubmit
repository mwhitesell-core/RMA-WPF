# letter_resubmit

echo Submitting "letter_resubmit" to run IN BATCH !!

cd $application_production
rm resubmit.ls  >/dev/null  2>/dev/null
batch << BATCH_EXIT
$cmd/elig_corrections_letters_resubmits 1>resubmit.ls 2>&1
BATCH_EXIT

echo Done submitting "letter_resubmit" to batch 
